import { Box } from "@mantine/core";
import React from "react";

const TrackPlayer = () => {
  return (
    <Box
      style={{
        marginTop: "40px",
        marginBottom: "20px",
        height: "80px",
        width: "100%",
        background: "#ccc",
      }}
    />
  );
};

export default TrackPlayer;
